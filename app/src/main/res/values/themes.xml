<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MyApplication" parent="Theme.MaterialComponents.DayNight.NoActionBar.Bridge">>
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->

    </style>
    <style name="MyListViewStyle" parent="Widget.AppCompat.ListView">
        <item name="android:background">@color/background_item</item>
        <!-- 你可以在这里添加其他属性，比如分隔符颜色等 -->
    </style>
    <style name="Theme.MyApplication" parent="Theme.AppCompat.DayNight.NoActionBar" >
        <item name="android:windowSplashScreenBackground">@android:color/white</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/alarm_clock_1</item>
        <item name="android:windowSplashScreenAnimationDuration">1000</item>
        <item name="android:listViewStyle">@style/MyListViewStyle</item>
    </style>
</resources>